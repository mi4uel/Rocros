{%- if product_image -%}
      <img
        src="{{ product_image | image_url: width: 1800 }}"
        alt="{{ product_image.alt | escape }}"
        class="motion-reduce visible"
        width="{{ product_image.width }}"
        height="{{ product_image.height }}"
      >
{%- endif -%}
